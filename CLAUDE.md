# abyss — Claude 向け作業規約

このファイルは Claude（ゲームバランス・実装担当）が毎回読む前提の常設ルール。
見た目・演出側の担当分担は [AGENTS.md](AGENTS.md) と [docs/VISUAL_DEVELOPMENT.md](docs/VISUAL_DEVELOPMENT.md)。

過去の引継ぎ書に毎回コピーされていた「環境の注意」「掃引の期待FAIL」「RNGの罠」を
ここ1箇所に集約した（2026-09-12）。引継ぎ書には**その回の変更内容だけ**を書き、
ここに書いてあることは再掲しない。

---

## 1. 役割分担

| | 担当 | 主な編集先 | 回帰確認 |
|---|---|---|---|
| **Claude** | ゲームバランス（ダメージ式・成長・報酬・敵AI） | `proto/index.html` | `./sweep.sh` |
| **Codex** | 見た目・動き・視覚フィードバック | `proto/game-feel.js` | `proto/feeltest.html`（ブラウザ） |

見た目の依頼に伴ってバランスを、バランスの依頼に伴って見た目を、独断で変えない。

**開発中の `proto/index.html` は単体では動かない。** `game-feel.js` を
`<script src>` で読み込む形になっている。人に配るときはインライン化して1ファイルに戻す
（「単一HTML・外部依存ゼロ」は出荷時の条件）。

---

## 2. 表示・コメントの語彙（退役語を書かない）

`loretest.mjs` の `R.oldWordsGone` が画面文字列から退役語を検出して掃引を止める。
**コメントに書いても引っかかる**ので、新規に書く文章もこちらに揃える。

| 書かない | 書く |
|---|---|
| 拠点 | **街** |
| ダンジョン／深淵 | **アビス** |
| マナ／秘石 | **SP**／**欠片** |
| プレイヤー（仲間の意味で） | **探索者** |
| 鉱石 | **黒鉄** |
| Magic（レア度） | **Uncommon／アンコモン** |

用語の全体像は project doc `claude/abyss-世界観設定書.md` 9章。

---

## 3. 掃引（sweep）

```
./sweep.sh      # 2回走らせて、出力が完全に一致することを確認する
```

- 回帰スイートは `proto/*test*.mjs`（**50本**・2026-09-12 時点）
- `proto/_*.mjs` は**測定ハーネスで、掃引には入らない**。合否を出さない
- テストの `R` 内の真偽値は「**true = 期待どおり**」に揃える約束。
  「当たらないのが正解」なら `hitBack:false` ではなく `sparesBehind: !hitBack` の形で返す

### 既知の期待FAIL（2026-09-09 時点の一覧）

新しい変更を入れたあと、これと**完全に一致**していればリグレッションなし。
ここから増えていたら、その項目が今回の変更のせい。

```
FAIL adtest    .reviveDone.dead
FAIL basetest  .durWarn.healthy.shown
FAIL fxtest    .noAutoHelp.helpShown
FAIL hubtest   .hub.townHasShopGrid
FAIL looptest  .shardSurvivesDeath.goldStillLost
FAIL pacetest  .bossHidden.revealed,.bossHidden.barShown,.bossDot.before,.bossPerFloor.secondRevealed
FAIL partytest .fallen.deadAfter,.uniqKill.blockedBefore
```

> この一覧は 2026-09-09 の状態。以降テストを書き換えた回があるので、
> **まず手を付ける前に掃引を2回走らせて現在の基準線を取る**こと。
> 基準線が変わっていたら、このブロックを更新する。

### 既知の不安定テスト（リグレッションではない）

| 項目 | 性質 |
|---|---|
| `looptest.live.alliesNeverStack` | 隊列間隔のシミュレーション。閾値ぎりぎりで確率的に揺れる |
| `movetest.R.orbit.holdsPosition` | 物理の実時間依存。閾値0.35に対し稀に0.35〜0.38 |

3回走らせて1回だけ落ちるものは、これに当たるか先に疑う。

---

## 4. 測定ハーネス（`proto/_*.mjs`）

| 道具 | 何を出すか |
|---|---|
| `_balance.mjs` | 累計SP別の到達階分布。階ごとの入場HP／最低HP／滞在秒／ボス残HP |
| `_wall.mjs` | ボス戦だけを取り出し「倒しきる秒数 ÷ 倒される秒数」の比。1.0未満で勝てる |
| `_perf.mjs` | 描画の内訳 |
| `_bb` / `_h` / `_intrperf` / `_shot` | 補助（撮影・個別計測） |

```
node proto/_balance.mjs --seeds 24 --sp 0,60,130,300,450 --maxdepth 20
node proto/_wall.mjs --depth 5 --sp 130
```

**数値を動かす前に、まずハーネスで測る。** 1本の走行で判断しない。

### 設計目標（ハーネスで見る基準値）

| 見るもの | 目標 |
|---|---|
| F5 入場HP（SP130） | 60〜70% |
| 5F の比（SP130） | 0.95〜1.05 |
| 到達階 中央値（SP0 / SP130 / SP450） | 2〜3F / 5F突破 / 10F突破 |
| 10F の比（SP450・段3） | 1.0 未満 |

> ⚠️ **この表は「自動操縦が」どこまで行けるかの基準値であって、人の体感ではない。**（2026-09-21）
>
> 実測では累計SP 60〜700 のどこでも到達階の**中央値が 5.0F から動かない**。
> 一方、作者の体感は「**10階までは丁度良いくらいのバランス**」。
> **食い違っているのは設計ではなく、測っている操縦者のほう。** 自動操縦は——
>
> - 予兆を避けない（このゲームは立ち位置だけで戦う設計なので、ここが致命的に効く）
> - 恩寵を選ばない／消耗品を使わない
> - 道中で武器を失うと**素手のまま走り続ける**（`BAL.equipBest` は鑑定済みしか着けない）
>
> なので **この表に届かないことを理由に、ゲーム側の数値を上げてはいけない。**
> 上げれば人にとっては簡単になる。上の目標値は
> 「**下手な操縦でもここまでは行けてほしい**」という下限の目安として読むこと。
>
> 「段3」と書いてあるのは `--dan 3` のこと。`--tier` は**階**を取る（0/5/10/20）。
> 混同すると黙って段1の世界を測る（`abyss-引継-評価の指摘を実装した回.md` 1節）。

> **ボス前に削られて入るのは仕様。**（2026-09-21・作者判断）
> 5Fの入場HPが 56〜75% なのは壁ではなく、そういう手触りにしてある。
> 篝火にあたる休憩地点をボス前に置く案は**採らない**。

---

## 5. 繰り返し踏んだ落とし穴（同じ轍を踏まない）

### 5.1 RNG ストリームの desync — これが一番多い

`rnd()` / `rf()` の呼び出しを1つ足し引きするだけで、以降に生成される階層が全部ずれる。
**シードを固定して前後を比べても、別の階層同士を比べている。**

- 正しい比べ方：階層を**1回だけ**生成してスナップショットし、実行時スイッチで
  両方の挙動を**同じ階層に対して**流す
- `buildItem()` は潜在の付帯抽選のせいで**消費回数そのものが結果依存**。
  テストで乱数消費に依存する値（開始位置の揺らぎ等）を使うなら、
  装備生成と乱数列を共有せず `RNG=mulberry32(専用シード)` で切り離す
- 「その日の出目」ではなく「設計」を測る。プレイヤーへの**約束**を検証するテストは、
  平均ではなく**上限**（多数引きの最大値）を相手にする

### 5.2 旧設計を固定していたテスト

設計を変えるとき、それを守っていたテストも一緒に書き換える必要がある。
過去に `looptest.shardCurve` / `fxtest.levelUp` / `gearttest` が該当した。
落ちたテストを「直す」前に、**そのテストが守っているのが旧設計でないか**を先に見る。

### 5.3 「必ず付く」＋ 具体的な % の指示

確率や抽選の指示で「必ず〜される」という前置きと具体的な % が**両方**出てきたら、
着手前に一言確認する：**その % は「付くか付かないか」の確率か、「付いたものの中身」の抽選重みか。**
（潜在システムでここを読み違えて3往復した。）

### 5.4 強さより先に回復の経路を疑う

敵の攻撃力を2倍にしても入場HPが 91%→89% しか動かなかった一方、
レベルアップの全回復を35%回復に変えた1行で 89%→77% 動いた。
消耗が生まれないときは、まず**回復・全快の経路**を探す。

### 5.5 小さい元絵に UI を合わせるとき

背景アート（171×300px など）に対して「柱を避ける」等を実装するなら、
元絵のピクセル解析だけで座標を決めない。**実際にレンダリングして
`getBoundingClientRect()` と突き合わせて検算する**（cover表示の倍率・オフセット・
flexレイアウトの実サイズは元絵からは追えない）。

---

## 6. 環境の制約（回避しようとしない）

| 事象 | 対処 |
|---|---|
| クラウドから `git push` が 403 | **恒久的に不可。** コミット／プッシュは Mac の GitHub Desktop を GUI 操作 |
| クラウドの `/root/abyss-repo` と Mac の `~/Documents/abyss` | git履歴は**別物**。**ファイル内容だけ**を同期している |
| `fonts.googleapis.com` / `cdn.playwright.dev` / npm / PyPI の一部 | egress ポリシーで 403。回避しない。必要な物はユーザーに添付してもらう |
| GUI 操作 | 画面ロック中は一切できない（screenshot だけは背景で通る） |
| `device_bash` がファイルを削除できない | `device_request_delete_permission` で許可を取る。取れなければ `_to_delete/` へ退避 |

---

## 7. Mac へ書き戻す前の手順（**必須**）

過去に一度、クラウド側の `index.html` を force-write して
Codex のコミット前の編集を消した。HEAD との比較だけでは作業ツリーの変更は見えない。

1. `git --no-optional-locks status --porcelain` で Mac 側の未コミット変更を見る
2. `proto/index.html` が `M` なら `git diff proto/index.html` を**読んでから**書く。
   自分が入れた覚えのない差分が1行でもあれば、**書かずに止める**
3. HEAD との比較だけで「衝突なし」と判断しない

`device_commit_files` には `expectedMtimeMs` を付けて、ユーザー側の新しい編集を踏まない。

---

## 8. GitHub Desktop の癖

- `device_commit_files` で書き戻した直後、**変更に気付いていないことがある。**
  Changes ⇄ History タブを往復すると再スキャンされる
- Changes の Filter テキストボックスやコミットの Summary/Description 欄は、
  AXTextField 表示に反して AXComboBox / AXTextArea だったり、`app_type` が
  全文置換でなく選択範囲挿入になることがある。文字数が食い違ったら `cmd+a` で
  選択範囲を確認し、`overwrite_existing:true` で全文置換し直す
- `.git/index.lock` が残るとコミットが失敗する。読むだけなら
  `git --no-optional-locks` でロックを作らない
- **コミット前に必ず Filter で対象ファイルを絞り、チェックが入っているのが
  意図したファイルだけであることを確認する**（未追跡ファイルは自動でチェックされる）

---

## 9. リポジトリの容量（2026-09-12 に整理）

- `.git` 158MB → **82MB**（未コミットのまま残っていた到達不能オブジェクトを `git gc --prune=now` で掃除）
- `.gitignore` に `/output/` `_to_delete/` `**/_to_delete/` `**/__pycache__/` を追加。
  `proto/_to_delete/_sync_tmp_*` の個別列挙38行はこれに置き換えて削除した
- `output/`（**501MB**）は未追跡の中間生成物。特に
  `output/combined-element-effects-v10〜v12/frames.json`（各31MB・v13で更新済み）は
  消してよい候補。消す前に対応する `docs/*_STUDY_*.md` を確認する
